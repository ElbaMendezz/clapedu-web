---
import { Image } from "astro:assets";
import faseExplorar from "../../assets/images/metodologia-fase-explorar.webp";
import faseCrear from "../../assets/images/metodologia-fase-crear.webp";
import faseActuar from "../../assets/images/metodologia-fase-actuar.webp";
import faseResolver from "../../assets/images/metodologia-fase-resolver.webp";

interface Tab {
  modifier: string;
  num: string;
  title: string;
  iconPaths: string;
}

const tabs: Tab[] = [
  {
    modifier: "explorar",
    num: "fase 01",
    title: "Explorar",
    iconPaths: '<circle cx="11" cy="11" r="8"></circle><path d="m21 21-4.3-4.3"></path>',
  },
  {
    modifier: "crear",
    num: "fase 02",
    title: "Crear",
    iconPaths:
      '<path d="M9 18h6"></path><path d="M10 22h4"></path><path d="M12 2a7 7 0 0 0-4 12.7c.6.6 1 1.3 1 2.3h6c0-1 .4-1.7 1-2.3A7 7 0 0 0 12 2Z"></path>',
  },
  {
    modifier: "actuar",
    num: "fase 03",
    title: "Actuar",
    iconPaths: '<path d="M22 2 11 13"></path><path d="M22 2 15 22l-4-9-9-4 20-7Z"></path>',
  },
  {
    modifier: "resolver",
    num: "fase 04",
    title: "Resolver",
    iconPaths:
      '<polyline points="22 7 13.5 15.5 8.5 10.5 2 17"></polyline><polyline points="16 7 22 7 22 13"></polyline>',
  },
];
---

<section class="metodologia" data-screen-label="Metodología">
  <div class="wrap">
    <div class="head">
      <div class="eyebrow" data-reveal>
        <span class="pulse-dot" aria-hidden="true">
          <span class="pulse-dot-ring"></span>
          <span class="pulse-dot-core"></span>
        </span>
        <span>Modelo de innovación social</span>
      </div>
      <h2 data-reveal data-metodh2>La Metodología CLAP</h2>
      <p data-reveal>
        El objetivo no es solo aprender conceptos. Es desarrollar la capacidad de liderar y ejecutar
        procesos de transformación, orientados a resultados.
      </p>
    </div>

    <div class="tabs" data-reveal data-phasetabs>
      {tabs.map((tab, i) => (
        <button
          type="button"
          class:list={["tab", `tab--${tab.modifier}`, { "is-active": i === 0 }]}
          data-phasetab={i}
        >
          <span class="tab-icon">
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" set:html={tab.iconPaths} />
          </span>
          <div>
            <span class="tab-num">{tab.num}</span>
            <div class="tab-title">{tab.title}</div>
          </div>
        </button>
      ))}
    </div>

    <div class="stage" data-reveal data-phasestage>
      <!-- PANEL 0 Explorar -->
      <div class="panel is-active" data-phasepanel="0">
        <div class="panel-text">
          <span class="panel-tag panel-tag--explorar">
            <span class="panel-tag-dot" aria-hidden="true"></span>
            fase 01 · explorar
          </span>
          <h3>Nos sumergimos en el contexto del problema</h3>
          <p>
            El innovador social investiga las necesidades de la comunidad y analiza oportunidades y
            desafíos. Aquí tomamos decisiones informadas y conectamos a los actores clave del territorio.
          </p>
          <div>
            <div class="comp-label">Competencias que activa</div>
            <div class="comp-tags">
              <span class="tag tag--analizar"><span class="tag-dot" aria-hidden="true"></span>Analizar</span>
              <span class="tag tag--articular"><span class="tag-dot" aria-hidden="true"></span>Articular</span>
            </div>
          </div>
        </div>
        <div class="panel-media">
          <Image src={faseExplorar} alt="" width={700} height={600} class="panel-photo" />
          <div class="panel-media-scrim scrim--explorar" aria-hidden="true"></div>
          <div class="panel-media-content">
            <div class="checklist-title">Diagnóstico territorial</div>
            <div class="check-row"><span class="check-mark">✓</span><span>Mapeo de actores y necesidades</span></div>
            <div class="check-row"><span class="check-mark">✓</span><span>Escucha activa con la comunidad</span></div>
            <div class="check-row"><span class="check-mark">✓</span><span>Definición del reto principal</span></div>
          </div>
        </div>
      </div>

      <!-- PANEL 1 Crear -->
      <div class="panel" data-phasepanel="1">
        <div class="panel-text">
          <span class="panel-tag panel-tag--crear">
            <span class="panel-tag-dot" aria-hidden="true"></span>
            fase 02 · crear
          </span>
          <h3>Diseñamos la estrategia y el plan de acción</h3>
          <p>
            Definimos objetivos claros alineados con las necesidades identificadas. Es crucial crear
            sinergias y coordinar recursos de manera efectiva para que la solución sea posible.
          </p>
          <div>
            <div class="comp-label">Competencias que activa</div>
            <div class="comp-tags">
              <span class="tag tag--articular"><span class="tag-dot" aria-hidden="true"></span>Articular</span>
              <span class="tag tag--gestionar"><span class="tag-dot" aria-hidden="true"></span>Gestionar</span>
            </div>
          </div>
        </div>
        <div class="panel-media panel-media--crear">
          <Image src={faseCrear} alt="" width={700} height={600} class="panel-photo" />
          <div class="panel-media-scrim scrim--crear" aria-hidden="true"></div>
          <div class="panel-media-content">
            <div class="checklist-title">Tablero de la estrategia</div>
            <div class="board-grid">
              <div class="board-card"><div class="board-bar board-bar--teal"></div><div class="board-card-title">Prototipo social</div></div>
              <div class="board-card"><div class="board-bar board-bar--yellow"></div><div class="board-card-title">Plan de impacto</div></div>
              <div class="board-card"><div class="board-bar board-bar--white"></div><div class="board-card-title">Aliados clave</div></div>
              <div class="board-card"><div class="board-bar board-bar--emerald"></div><div class="board-card-title">Presupuesto</div></div>
            </div>
          </div>
        </div>
      </div>

      <!-- PANEL 2 Actuar -->
      <div class="panel" data-phasepanel="2">
        <div class="panel-text">
          <span class="panel-tag panel-tag--actuar">
            <span class="panel-tag-dot" aria-hidden="true"></span>
            fase 03 · actuar
          </span>
          <h3>Convertimos la planificación en acción</h3>
          <p>
            Implementamos el plan, aplicamos las estrategias y monitoreamos el progreso. Dirigimos la
            iniciativa con eficiencia y adaptamos las estrategias en territorio según sea necesario.
          </p>
          <div>
            <div class="comp-label">Competencias que activa</div>
            <div class="comp-tags">
              <span class="tag tag--gestionar"><span class="tag-dot" aria-hidden="true"></span>Gestionar</span>
              <span class="tag tag--intervenir"><span class="tag-dot" aria-hidden="true"></span>Intervenir</span>
            </div>
          </div>
        </div>
        <div class="panel-media">
          <Image src={faseActuar} alt="" width={700} height={600} class="panel-photo" />
          <div class="panel-media-scrim scrim--actuar" aria-hidden="true"></div>
          <div class="panel-media-content">
            <div class="action-head">
              <div class="action-title">Plan de acción</div>
              <span class="action-badge">En curso</span>
            </div>
            <div class="action-row"><span class="action-check">✓</span><span>Conformación del equipo local</span></div>
            <div class="action-row"><span class="action-check">✓</span><span>Puesta en marcha del piloto</span></div>
            <div class="action-row"><span class="action-check action-check--soft">✓</span><span>Acompañamiento en campo</span></div>
          </div>
        </div>
      </div>

      <!-- PANEL 3 Resolver -->
      <div class="panel" data-phasepanel="3">
        <div class="panel-text">
          <span class="panel-tag panel-tag--resolver">
            <span class="panel-tag-dot" aria-hidden="true"></span>
            fase 04 · resolver
          </span>
          <h3>Medimos el impacto y escalamos lo que funciona</h3>
          <p>
            Evaluamos el impacto del proyecto, medimos los resultados y los comparamos con los objetivos
            iniciales. Sistematizamos las lecciones aprendidas para mejorar futuras intervenciones.
          </p>
          <div>
            <div class="comp-label">Competencias que activa</div>
            <div class="comp-tags">
              <span class="tag tag--intervenir"><span class="tag-dot" aria-hidden="true"></span>Intervenir</span>
              <span class="tag tag--analizar"><span class="tag-dot" aria-hidden="true"></span>Analizar</span>
            </div>
          </div>
        </div>
        <div class="panel-media">
          <Image src={faseResolver} alt="" width={700} height={600} class="panel-photo" />
          <div class="panel-media-scrim scrim--resolver" aria-hidden="true"></div>
          <div class="panel-media-content">
            <div class="checklist-title">Resultados de impacto</div>
            <div class="impact-stats">
              <div><div class="impact-num impact-num--teal">+120</div><div class="impact-label">proyectos en territorio</div></div>
              <div><div class="impact-num impact-num--yellow">8K</div><div class="impact-label">personas alcanzadas</div></div>
              <div><div class="impact-num impact-num--white">94%</div><div class="impact-label">continúan liderando</div></div>
            </div>
            <div class="impact-bar"><div class="impact-bar-fill"></div></div>
          </div>
        </div>
      </div>
    </div>

    <div class="cta-wrap" data-reveal>
      <a class="cta-link" href="/metodologia">
        Conoce la metodología completa
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path></svg>
      </a>
    </div>
  </div>
</section>

<script>
  import { attachReveal } from "../../scripts/reveal";
  import { attachPhaseStepper } from "../../scripts/phase-stepper";

  const section = document.querySelector<HTMLElement>(".metodologia");
  if (section) {
    attachReveal(section, {
      threshold: 0.15,
      rootMargin: "0px 0px -12% 0px",
      repeat: true,
      duration: 950,
      distanceY: 46,
      distanceX: 58,
      easing: "cubic-bezier(.16,.8,.3,1)",
    });
    attachPhaseStepper(section);
  }
</script>

<style>
  .metodologia {
    background: linear-gradient(180deg, #fff 0%, #f7f4fc 100%);
    padding: 96px 48px 100px;
  }

  .wrap {
    max-width: 1160px;
    margin: 0 auto;
  }

  .head {
    text-align: center;
    max-width: 660px;
    margin: 0 auto 12px;
  }
  .eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 9px;
    padding: 8px 17px;
    border: 1px solid rgba(19, 184, 154, 0.4);
    border-radius: 40px;
    background: rgba(19, 184, 154, 0.1);
    font-family: var(--font-label);
    font-size: 12.5px;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: #341c65;
    font-weight: 400;
  }
  .pulse-dot {
    position: relative;
    display: flex;
    width: 8px;
    height: 8px;
  }
  .pulse-dot-ring {
    position: absolute;
    inset: 0;
    border-radius: 50%;
    background: #13b89a;
    animation: pe-pulse 2.2s ease-out infinite;
  }
  .pulse-dot-core {
    position: relative;
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background-color: #13b89a;
  }
  @keyframes pe-pulse {
    0% { transform: scale(0.6); opacity: 0.5; }
    80% { opacity: 0; }
    100% { transform: scale(1.7); opacity: 0; }
  }

  h2 {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: 44px;
    color: #341c65;
    line-height: 1.06;
    letter-spacing: -0.026em;
    margin-top: 12px;
    text-wrap: balance;
  }
  .head p {
    font-size: 16px;
    line-height: 1.6;
    color: #6b6786;
    margin-top: 16px;
  }

  /* ---------- tabs ---------- */
  .tabs {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 10px;
    margin-top: 44px;
    margin-bottom: 38px;
  }

  .tab {
    position: relative;
    text-align: left;
    border: 1.5px solid rgba(52, 28, 101, 0.13);
    background: transparent;
    border-radius: 16px;
    padding: 12px 14px;
    cursor: pointer;
    font-family: var(--font-body);
    display: flex;
    align-items: center;
    gap: 13px;
    color: #9a93ad;
    transition: background 0.35s, color 0.35s, border-color 0.35s, box-shadow 0.35s, transform 0.35s;
  }
  .tab.is-active {
    background: #fff;
    color: #341c65;
    border-color: rgba(52, 28, 101, 0.06);
    box-shadow: 0 4px 16px -6px rgba(52, 28, 101, 0.1);
    transform: translateY(-2px);
  }

  .tab-icon {
    flex: none;
    width: 52px;
    height: 52px;
    border-radius: 14px;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: background 0.35s, color 0.35s, box-shadow 0.35s;
  }
  .tab--explorar .tab-icon {
    background: rgba(240, 98, 146, 0.12);
    color: #f06292;
  }
  .tab--explorar.is-active .tab-icon {
    background: #f06292;
    color: #fff;
    box-shadow: 0 8px 22px -10px rgba(240, 98, 146, 0.7);
  }
  .tab--crear .tab-icon {
    background: rgba(245, 158, 11, 0.12);
    color: #f59e0b;
  }
  .tab--crear.is-active .tab-icon {
    background: #f59e0b;
    color: #fff;
    box-shadow: 0 8px 22px -10px rgba(245, 158, 11, 0.7);
  }
  .tab--actuar .tab-icon {
    background: rgba(34, 197, 94, 0.12);
    color: #22c55e;
  }
  .tab--actuar.is-active .tab-icon {
    background: #22c55e;
    color: #fff;
    box-shadow: 0 8px 22px -10px rgba(34, 197, 94, 0.7);
  }
  .tab--resolver .tab-icon {
    background: rgba(59, 130, 246, 0.12);
    color: #3b82f6;
  }
  .tab--resolver.is-active .tab-icon {
    background: #3b82f6;
    color: #fff;
    box-shadow: 0 8px 22px -10px rgba(59, 130, 246, 0.7);
  }

  .tab-num {
    display: block;
    font-family: var(--font-label);
    font-size: 11px;
    margin-bottom: 3px;
    opacity: 0.65;
  }
  .tab-title {
    font-family: var(--font-display);
    font-weight: 800;
    font-size: 16px;
    letter-spacing: -0.01em;
  }

  /* ---------- stage / panels ---------- */
  .stage {
    position: relative;
    min-height: 380px;
  }

  .panel {
    display: none;
    grid-template-columns: 1.15fr 0.85fr;
    border-radius: 28px;
    overflow: hidden;
    background: #fff;
    box-shadow: 0 34px 80px -44px rgba(52, 28, 101, 0.5);
    opacity: 0;
    transform: translateY(14px);
  }
  .panel.is-active {
    display: grid;
  }
  .panel.is-visible {
    opacity: 1;
    transform: translateY(0);
    transition: opacity 0.5s ease, transform 0.5s cubic-bezier(0.2, 0.7, 0.2, 1);
  }

  .panel-text {
    padding: 44px 46px;
  }
  .panel-tag {
    display: inline-flex;
    align-items: center;
    gap: 9px;
    padding: 6px 14px;
    border-radius: 30px;
    margin-bottom: 18px;
    font-family: var(--font-label);
    font-size: 12px;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }
  .panel-tag-dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
  }
  .panel-tag--explorar {
    background: rgba(240, 98, 146, 0.12);
    color: #b83470;
  }
  .panel-tag--explorar .panel-tag-dot {
    background: #f06292;
  }
  .panel-tag--crear {
    background: rgba(245, 158, 11, 0.12);
    color: #b56b00;
  }
  .panel-tag--crear .panel-tag-dot {
    background: #f59e0b;
  }
  .panel-tag--actuar {
    background: rgba(34, 197, 94, 0.12);
    color: #16843f;
  }
  .panel-tag--actuar .panel-tag-dot {
    background: #22c55e;
  }
  .panel-tag--resolver {
    background: rgba(59, 130, 246, 0.12);
    color: #1d4ed8;
  }
  .panel-tag--resolver .panel-tag-dot {
    background: #3b82f6;
  }

  .panel-text h3 {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: 30px;
    letter-spacing: -0.02em;
    color: #341c65;
    line-height: 1.12;
    text-wrap: balance;
  }
  .panel-text p {
    font-size: 15.5px;
    line-height: 1.7;
    color: #6b6786;
    margin-top: 14px;
  }
  .comp-label {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 12.5px;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: #9a93ad;
    margin-bottom: 12px;
  }
  .comp-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 26px;
  }
  .tag {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 9px 16px;
    border-radius: 30px;
    border: 1px solid transparent;
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 13.5px;
  }
  .tag-dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
  }
  .tag--analizar {
    background: rgba(240, 98, 146, 0.1);
    border-color: rgba(240, 98, 146, 0.3);
    color: #b83470;
  }
  .tag--analizar .tag-dot {
    background: #f06292;
  }
  .tag--articular {
    background: rgba(232, 99, 10, 0.1);
    border-color: rgba(232, 99, 10, 0.3);
    color: #b84c00;
  }
  .tag--articular .tag-dot {
    background: #e8630a;
  }
  .tag--gestionar {
    background: rgba(34, 197, 94, 0.1);
    border-color: rgba(34, 197, 94, 0.3);
    color: #16843f;
  }
  .tag--gestionar .tag-dot {
    background: #22c55e;
  }
  .tag--intervenir {
    background: rgba(59, 130, 246, 0.1);
    border-color: rgba(59, 130, 246, 0.3);
    color: #2563eb;
  }
  .tag--intervenir .tag-dot {
    background: #3b82f6;
  }

  /* ---------- panel media ---------- */
  .panel-media {
    position: relative;
    padding: 40px 38px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    overflow: hidden;
  }
  .panel-media :global(.panel-photo) {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    display: block;
  }
  .panel-media--crear :global(.panel-photo) {
    object-position: center top;
  }
  .panel-media-scrim {
    position: absolute;
    inset: 0;
  }
  .scrim--explorar {
    background: linear-gradient(155deg, rgba(11, 143, 118, 0.66) 0%, rgba(7, 90, 75, 0.58) 50%, rgba(13, 50, 86, 0.72) 100%);
  }
  .scrim--crear {
    background: linear-gradient(155deg, rgba(94, 44, 204, 0.66) 0%, rgba(67, 25, 124, 0.6) 55%, rgba(13, 50, 86, 0.72) 100%);
  }
  .scrim--actuar {
    background: linear-gradient(155deg, rgba(22, 132, 63, 0.66) 0%, rgba(14, 98, 46, 0.6) 55%, rgba(13, 50, 86, 0.72) 100%);
  }
  .scrim--resolver {
    background: linear-gradient(155deg, rgba(13, 50, 86, 0.72) 0%, rgba(52, 28, 101, 0.64) 55%, rgba(19, 184, 154, 0.55) 100%);
  }
  .panel-media-content {
    position: relative;
  }

  .checklist-title {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 13px;
    color: rgba(255, 255, 255, 0.85);
    margin-bottom: 16px;
  }
  .check-row {
    display: flex;
    align-items: center;
    gap: 11px;
    padding: 11px 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.14);
  }
  .check-row:last-child {
    border-bottom: none;
  }
  .check-mark {
    width: 24px;
    height: 24px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.92);
    color: #0b8f76;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 13px;
    flex: none;
  }
  .check-row span:last-child {
    font-size: 14px;
    color: #fff;
  }

  .board-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 11px;
  }
  .board-card {
    background: rgba(255, 255, 255, 0.12);
    border-radius: 13px;
    padding: 13px 15px;
  }
  .board-bar {
    width: 28px;
    height: 6px;
    border-radius: 4px;
    margin-bottom: 9px;
  }
  .board-bar--teal {
    background: #3be0bb;
  }
  .board-bar--yellow {
    background: #ffc200;
  }
  .board-bar--white {
    background: #fff;
  }
  .board-bar--emerald {
    background: #13b89a;
  }
  .board-card-title {
    font-size: 13px;
    font-weight: 700;
    color: #fff;
  }

  .action-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 16px;
  }
  .action-title {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 13px;
    color: #fff;
  }
  .action-badge {
    font-size: 11px;
    font-weight: 700;
    color: #c77a0a;
    background: #fff;
    padding: 4px 10px;
    border-radius: 20px;
  }
  .action-row {
    display: flex;
    align-items: center;
    gap: 11px;
    padding: 10px 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.16);
  }
  .action-row:last-child {
    border-bottom: none;
  }
  .action-check {
    width: 22px;
    height: 22px;
    border-radius: 50%;
    background: #fff;
    color: #c77a0a;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 12px;
    flex: none;
  }
  .action-check--soft {
    background: rgba(255, 255, 255, 0.92);
  }
  .action-row span:last-child {
    font-size: 13.5px;
    color: #fff;
  }

  .impact-stats {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 14px;
  }
  .impact-num {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: 28px;
  }
  .impact-num--teal {
    color: #3be0bb;
  }
  .impact-num--yellow {
    color: #ffc200;
  }
  .impact-num--white {
    color: #fff;
  }
  .impact-label {
    font-size: 11.5px;
    color: rgba(255, 255, 255, 0.78);
    line-height: 1.4;
    margin-top: 2px;
  }
  .impact-bar {
    margin-top: 20px;
    height: 8px;
    border-radius: 6px;
    background: rgba(255, 255, 255, 0.18);
    overflow: hidden;
  }
  .impact-bar-fill {
    width: 84%;
    height: 100%;
    background: linear-gradient(90deg, #3be0bb, #ffc200);
  }

  /* ---------- CTA ---------- */
  .cta-wrap {
    display: flex;
    justify-content: center;
    margin-top: 34px;
  }
  .cta-link {
    display: inline-flex;
    align-items: center;
    gap: 9px;
    text-align: center;
    color: #43197c;
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 15px;
    padding: 14px 28px;
    border-radius: 40px;
    text-decoration: none;
    border: 1px solid rgba(67, 25, 124, 0.22);
    transition: background 0.25s, transform 0.25s;
  }
  .cta-link:hover {
    background: #f1ecfa;
    transform: translateY(-2px);
  }

  /* ---------- responsive ---------- */
  @media (max-width: 559.98px) {
    .metodologia {
      padding-top: 48px;
      padding-bottom: 52px;
    }
    h2 {
      font-size: 28px;
    }
    .tabs {
      gap: 7px;
    }
    .tab {
      flex-direction: column;
      align-items: center;
      text-align: center;
      padding: 11px 4px;
      gap: 7px;
    }
    .tab-icon {
      width: 34px;
      height: 34px;
    }
    .tab-num {
      display: none;
    }
    .tab-title {
      font-size: 11px;
    }
  }
  @media (max-width: 759.98px) {
    .panel {
      grid-template-columns: 1fr;
    }
  }
</style>
